<div>
  {{-- <iframe
  width="{{ $width }}"
  height="{{ $height }}"
  src="{{ $url }}"
  title="{{ $title }}"
  frameborder="0"
  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
  allowfullscreen
  loading="lazy"
></iframe> --}}
  {{ $url }}
  {{ $origin }}
</div>
